<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- WEEK 7: views for the library.category model (Many2many target) -->

    <!-- List View -->
    <record id="view_library_category_list" model="ir.ui.view">
        <field name="name">library.category.list</field>
        <field name="model">library.category</field>
        <field name="arch" type="xml">
            <list string="Categories" editable="bottom">
                <field name="name"/>
                <field name="description"/>
                <field name="book_count"/>
                <field name="color" widget="color_picker"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_library_category_form" model="ir.ui.view">
        <field name="name">library.category.form</field>
        <field name="model">library.category</field>
        <field name="arch" type="xml">
            <form string="Category">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color" widget="color_picker"/>
                        <field name="book_count"/>
                    </group>
                    <field name="description" placeholder="Describe this category..."/>
                    <!-- Many2many widget: editing this list emits the special
                         ORM commands (link/unlink/create) under the hood. -->
                    <field name="book_ids" widget="many2many_tags"/>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_library_category" model="ir.actions.act_window">
        <field name="name">Categories</field>
        <field name="res_model">library.category</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first category!
            </p>
            <p>
                Categories let you tag and classify books (Fiction, Science, ...).
            </p>
        </field>
    </record>
</odoo>
